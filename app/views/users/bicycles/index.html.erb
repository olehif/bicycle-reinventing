<%= form_tag users_bicycles_path, method: :get, remote: true do %>
  <%= text_field_tag :search %>
  <%= select_tag :bicycle_category_id,
                 options_from_collection_for_select(BicycleCategory.all, :id, :title),
                 include_blank: true %>
  <%= submit_tag 'Search' %>
<% end %>

My bicycles:
<%= render 'shared/bicycles_list' %>